

export const metadata = {
  title: "About Miyabi House",
  description: "Japanese Restaurant About Page",
};

export default function AboutLayout({ children }) {
  return (
   
      <div>
        {children}
      </div>
  
  );
}
